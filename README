# ios-libery-design-system

Este repositório é um exemplo de como criar e estruturar uma biblioteca iOS usando CocoaPods, para ser facilmente integrada em outros projetos iOS. A biblioteca "libery" é um design system que pode ser usado como base para construir aplicativos iOS consistentes e de alta qualidade.

## Uso

Para usar esta biblioteca em seu projeto, adicione a seguinte linha ao seu arquivo Podfile:

```ruby
pod 'libery', :git => 'https://github.com/thalespa/ios-libery-design-system.git'
